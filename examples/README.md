# Examples

This repository keeps its example study synthetic and generated on demand.

Create the demo workspace with:

```bash
tiktok-research init-demo /path/to/demo-workspace
```

That command writes a complete example workspace outside the repo using synthetic metadata, transcripts, workbook rows, and calibration candidates.
